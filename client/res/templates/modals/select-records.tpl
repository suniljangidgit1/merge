{{#if createButton}}
<div class="button-container">
    <button class="btn btn-primary" data-action="create"><i class="material-icons-outlined" style="font-size: 13px;position: relative;top: 2px;">add</i> {{createText}}</button>
</div>
{{/if}}
<div class="search-container">{{{search}}}</div>
<div class="list-container">{{{list}}}</div>